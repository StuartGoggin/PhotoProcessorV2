{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main window to open folder picker dialogs",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
